[package]
name = "stub_program"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[features]
idl-build = []
custom-heap = []
custom-panic = []

[dependencies]
solana-program = "2.3"
